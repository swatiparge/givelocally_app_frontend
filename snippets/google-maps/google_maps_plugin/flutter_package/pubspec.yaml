name: google_maps_plugin
description: "A reusable Google Maps module with Unified Map View and Location Picker."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  cloud_functions: ^4.6.0
  firebase_core: ^2.24.0
  permission_handler: ^11.0.0
  flutter_image_compress: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
